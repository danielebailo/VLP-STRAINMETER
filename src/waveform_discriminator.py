"""Discriminator phase placeholder.

The current project phase stops at data loading, autoencoder construction,
and training. The reconstruction-error discriminator will be implemented later.
"""


class WaveformDiscriminator:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "WaveformDiscriminator is intentionally not implemented in this phase."
        )
